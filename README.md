# vite-collect

### Create Vite App

```
npx create-vite-app <project-name>
cd <project-name>
npm install
npm run dev
```

### Quick git push

```
git add . && git commit -m "initialCommit" && git push
```
